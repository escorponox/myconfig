# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

#monitors
# DP1-8 middle DP1-1 left HDMI2 right
exec --no-startup-id xrandr --output DP1-1 --left-of DP1-8 --auto --output HDMI2 --right-of DP1-8 --auto
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1 &

#exec sudo setpci -s 00:02.0 F4.B=FF
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

#focus_follows_mouse no
#mouse_warping none

# start a terminal
bindsym $mod+Return exec tilix

bindsym XF86AudioRaiseVolume exec amixer -q set Master 5+
bindsym XF86AudioLowerVolume exec amixer -q set Master 5-

bindsym $mod+y exec google-chrome-stable
bindsym $mod+u exec nemo
bindsym $mod+i exec "/home/escorponox/idea-IU/bin/idea.sh"
bindsym $mod+o exec oni 
bindsym $mod+p exec shutter
bindsym $mod+m exec "setxkbmap -model pc104 -layout us,es -variant ,, -option grp:menu_toggle"

# kill focused window
bindsym $mod+space kill

# start dmenu (a program launcher)
bindsym $mod+Shift+d exec dmenu_run
bindsym $mod+d exec j4-dmenu-desktop
#bindsym $mod+Tab exec i3-winmenu.py

#bindsym $mod+d exec "rofi -combi-modi drun,run -show combi -modi combi"
bindsym $mod+Tab exec "rofi -show window"

# change focus
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+h focus left

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Switch window/workspace with mod+scroll and mod+shift scroll
bindsym --whole-window --border $mod+button4 focus left
bindsym --whole-window --border $mod+button5 focus right
bindsym button4 nop
bindsym button5 nop

bindsym $mod+Mod1+Left workspace prev
bindsym $mod+Mod1+Right workspace next
bindsym $mod+control+Left workspace prev_on_output
bindsym $mod+control+Right workspace next_on_output

bindsym $mod+control+Shift+Left focus output DVI-D-0
bindsym $mod+control+Shift+Right focus output HDMI-0

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+b split horizontal 

# split in vertical orientation
bindsym $mod+Shift+v split vertical

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# workspace definitions
workspace_layout tabbed
hide_edge_borders both

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+q layout stacking
bindsym $mod+a layout tabbed
bindsym $mod+s layout toggle split

# toggle tiling / floating
bindsym $mod+t floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+t focus mode_toggle

# focus the parent container
bindsym $mod+Shift+p focus parent

# focus the child container
bindsym $mod+Shift+c focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+q workspace q
bindsym $mod+2 workspace 2
bindsym $mod+w workspace w
bindsym $mod+3 workspace 3
bindsym $mod+e workspace e

workspace 1 output DP1-1
workspace q output DP1-1
workspace 2 output DP1-8
workspace w output DP1-8
workspace 3 output HDMI2
workspace e output HDMI2

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+q move container to workspace q
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+w move container to workspace w
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+e move container to workspace e

#Make the currently focused window a scratchpad
bindsym $mod+Shift+z move scratchpad

# Show the first scratchpad window
bindsym $mod+z scratchpad show, resize set 2300 1300, move position center

# window rules
for_window [class="Gpick"] floating enable border normal
for_window [class="notes" instance="notes"] floating enable border none
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [class="xfce4-notes" instance="Xfce4-notes"] floating enable border none
for_window [class="^jetbrains-.+"][window_type=dialog] focus
for_window [class="tilix"] resize set 2400 1300 move set 100 100
for_window [class="Google-chrome"] floating disable

assign [class="VirtualBox"] e
assign [class="^VirtualBox\sMachine$"] e

# reload the configuration file
bindsym $mod+F12 reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+F12 restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Escape exec "i3-nagbar -t warning -m 'You pressed poweroff. Do you really want to turn off the computer?' -b 'Poweroff' 'systemctl poweroff' -b 'Reboot' 'systemctl reboot' -b 'LogOut' 'i3-msg exit' -b 'Suspend' 'systemctl suspend'"
# dpms screen off
bindsym $mod+0 exec "sleep 1; xset dpms force off"

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id "sleep 1; xset dpms force off", mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym h exec --no-startup-id systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Escape mode "$mode_system"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# barra del monitor central
bar {
        id bar-2
        #status_command i3status
        status_command /home/escorponox/.i3/conky-i3bar
        position top
#        mode hide
#        hidden_state hide
        #modifier Mod4
        output DP1-8
        font pango:DejaVu Sans Mono 12
#        tray_output HDMI-0
}
#barra del monitor rightl
bar {
        id bar-1
        position top
#        mode hide
#        hidden_state hide
#        modifier Mod4
        output HDMI2
        font pango:DejaVu Sans Mono 12
        tray_output none
}
#barra del left
bar {
        id bar-3
        position top
#        mode hide
#        hidden_state hide
#        modifier Mod4
        output DP1-1
        font pango:DejaVu Sans Mono 12
        tray_output none
}

exec --no-startup-id xsetroot -solid "#222222"
exec --no-startup-id nm-applet
exec --no-startup-id numlockx
exec --no-startup-id dunst -to 5 -mon 1 -follow none -history_key F10
exec --no-startup-id pasystray
exec --no-startup-id xfce4-notes
exec --no-startup-id rofi -key-window mod4+Tab
exec --no-startup-id "setxkbmap -model pc104 -layout us,es -variant ,, -option grp:menu_toggle"
